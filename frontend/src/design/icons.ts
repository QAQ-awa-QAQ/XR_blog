/**
 * 侧栏导航图标。
 *
 * 为什么单独放一个文件：图标是**设计参数**——它决定了一个按钮的视觉重量，
 * 以及四个按钮放在一起时能不能靠外形互相区分。调它不该需要动 Sidebar.tsx
 * 的渲染逻辑，也不该埋在组件内部。
 *
 * 渲染口径与 `pages/main/sections.tsx` 的功能卡片图标完全一致：
 * 24 单位视图框、只描边不填充、`stroke-width: 1.7`、端点与拐角都是圆头。
 *
 * 选型依据（ui-ux-pro-max，icons 域 → Phosphor Outline 一族）：
 * 四个图标在 24px 下必须能靠**外轮廓**分辨，所以刻意选了四种剪影 ——
 * 尖顶（首页）/ 四块（功能）/ 横长方形（联系）/ 横向三点（更多，见 morph.ts）。
 * 24px 这个尺寸与描边宽度（1.7 单位）是配套的：面板宽度不变，只放大图标，
 * 描边实际粗细会跟着变大，视觉重量得靠这一点补回来。
 */

/** 所有图标共用的视图框边长 */
export const ICON_VIEWBOX = 24

/** 图标的基础渲染尺寸（px）。这是**选中态**的尺寸 */
export const ICON_SIZE = 24

/** **未选中**态的缩放倍率（选中时回到 1）。

    两个方向一起动才有「选中」的力度：图标「收」进去的同时胶囊「弹」出来。
    1.2 = 比选中态大 20%。

    早先用 5%（25.2 / 24）表达这个差异，实测**根本看不出来** —— 1.2px 的差别，
    而且切换是瞬时的、没有过程。现在改成 scale 倍率 + 过渡：
      · 倍率而不是两套绝对值 —— 基准尺寸只写一次；
      · `transform: scale()` 是合成层变换，不触发布局，所以能平滑过渡。

    倍率由 Sidebar 写成 CSS 变量交给样式表。 */
export const ICON_SCALE_IDLE = 1.2

export const NAV_ICON_PATHS = {
  /** 首页：屋顶 + 两墙 + 门 */
  intro: [
    'M3 10.4 12 3.6l8.5 6.8V20a1 1 0 0 1-1 1H4a1 1 0 0 1-1-1z',
    'M10 21v-6a1 1 0 0 1 1-1h2a1 1 0 0 1 1 1v6',
  ],
  /** 功能：2×2 圆角方块。间距 4 单位是下限——再挤，24px 下描边会糊成一块 */
  features: [
    'M4 2h4a2 2 0 0 1 2 2v4a2 2 0 0 1-2 2H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2z',
    'M16 2h4a2 2 0 0 1 2 2v4a2 2 0 0 1-2 2h-4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2z',
    'M4 14h4a2 2 0 0 1 2 2v4a2 2 0 0 1-2 2H4a2 2 0 0 1-2-2v-4a2 2 0 0 1 2-2z',
    'M16 14h4a2 2 0 0 1 2 2v4a2 2 0 0 1-2 2h-4a2 2 0 0 1-2-2v-4a2 2 0 0 1 2-2z',
  ],
  /** 联系：信封 + 封口 */
  contact: [
    'M3 7.5A2.5 2.5 0 0 1 5.5 5h13A2.5 2.5 0 0 1 21 7.5v9a2.5 2.5 0 0 1-2.5 2.5h-13A2.5 2.5 0 0 1 3 16.5z',
    'M3.8 6.5 12 12.6 20.2 6.5',
  ],
  /** 更多：横向三点。
      半径 0.85 配 1.7 的描边宽度 = 直径 3.4 的实心点，与 `morph.ts` 里那套
      归档坐标（3.4 见方、rx=1.7 的方块）是同一个形状 ——
      所以第四个按钮的字形与旧版「更多」按钮无缝衔接，只是它现在是一个页面入口 */
  more: [
    'M4.7 12a0.85 0.85 0 1 0 1.7 0a0.85 0.85 0 1 0-1.7 0',
    'M12 12a0.85 0.85 0 1 0 1.7 0a0.85 0.85 0 1 0-1.7 0',
    'M19.3 12a0.85 0.85 0 1 0 1.7 0a0.85 0.85 0 1 0-1.7 0',
  ],
} as const satisfies Record<string, readonly string[]>

/** 导航分区的键。这是该联合类型的唯一来源，Sidebar 的 SectionId 直接复用它 */
export type NavSectionKey = keyof typeof NAV_ICON_PATHS
