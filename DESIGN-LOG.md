# 决策日志

按日期追加的**决策与依据**记录。三条规矩：

1. **每条都带数值和理由** —— 三个月后回看，能知道当初为什么这么定，而不是重新想一遍。
2. **否决的方案也要写** —— 试过什么、为什么不用。撤回的记录比采纳的记录更省事。
3. **只追加，不修改历史条目** —— 改变主意就新开一条，并注明它取代了哪条。

需求本身见 `design.md`（唯一需求源），执行方案见 `plan.md`。

---

## 2026-09-11 · 需求澄清（18 项）

开工前逐条确认，避免"猜需求"。要点与理由：

| 项 | 决策 | 影响 |
|---|---|---|
| 前端 | React 19 + Vite + TS + GSAP | 不装 router：切换靠状态，`/admin` 用 pathname 判断，省一个依赖 |
| 后端 | Go + Gin + GORM + SQLite（纯 Go driver） | 免 CGO，容器内静态构建 |
| 容器 | 单容器内 Nginx + Go 双进程 | 前端产物由 Nginx 直接托管，无需跨容器 |
| 注册 | 开放注册 **+ 邀请码必填** + 分角色 | 邀请码一次性、7 天有效；admin 由冷启动创建，因此不存在"要先有码才能建码"的死锁 |
| 会话 | Cookie 存 session id，服务端 Redis 会话表 | 而非无状态 JWT —— 便于即时踢下线 |
| 密码 | argon2id（m=64MB, t=1, p=4） | 账号不存在时也跑一次哈希，避免用响应时间枚举账号 |
| 封禁 | 首次 24h、再犯永久 | 因此 `offense_count` **永不清除**，否则重启后无法区分首次与再犯 |
| 邀请码 | 16 位、去掉易混字符（0/O/1/I/l） | 便于口头或手抄传递 |

**否决**：暗色模式切换、评论、搜索、订阅、i18n、后台数据看板。理由：`design.md` 未提，未提即不做。

---

## 2026-09-11 · 安全规则的两处模型修正

### 1. 4.1 与 4.2 原本互斥

4.1 写"冷却期间不接收该 IP 的任何请求"，4.2 又要求"20 秒内 20 次"。冷却期若完全不接收、不计数，**20 次阈值永远达不到**，4.2 是一条死规则。

修正：冷却期返回 429，但**仍然计数**。这样两条规则才可共存。

### 2. 闸门与计数的职责边界

Nginx `auth_request` 只把 401/403 当作拒绝，返回 429 会被当成 500。所以闸门统一返回 **403 + `X-Block: banned|cooldown`**，由 `@blocked` 决定跳向哪一页。

**实测踩到的坑**：闸门最初把冷期间的 API 请求也一起 302 走了，20 次请求里只有前 3 次到达 Go，计数停在 3 —— 4.2 在真实部署中彻底失效。修正：`/api/auth/login` 与 `/api/auth/register` **豁免闸门**，改由 Go 的 `ipguard` 判定。

**禁止**在 Nginx 上加 `limit_req`：Nginx 若先返回 503，Go 同样数不到 20 次。阈值判定只放在 Go 里。

> 教训：Go 单测 24 项全绿，也测不出这个缺陷 —— 它跨了 Nginx 与 Go 两层。跨层行为必须真机验证。

---

## 2026-09-11 · 部署三处修正

| 问题 | 修正 | 理由 |
|---|---|---|
| 镜像构建失败 | `ARG GOPROXY` 必须声明在 `go mod download` **之前** | `ARG` 只对声明之后的 `RUN` 生效；容器内直连 `proxy.golang.org` 是 connection refused |
| 重定向丢端口 | `absolute_redirect off;` | 容器内监听 80、对外映射到其他端口时，默认生成的绝对地址会丢掉端口号 |
| 宿主机改库不生效 | 改完必须 `docker compose restart app` | Docker Desktop(Windows) 的文件共享层有缓存，容器看不到宿主侧的写入；排查时一度误判为封禁逻辑有错 |

---

## 2026-09-11 · 视觉与运动基线

视觉基线来自 `ui-ux-pro-max` 生成的 `design-system/xr-personal-blog/MASTER.md`（Liquid Glass / Inter / blur 18px / 玻璃膜 0.15）。

### 关键改动：整屏切换改用**临界阻尼弹簧**，废弃"事件 + 时间锁"

- 旧实现：滚轮事件 → `goTo(index±1)` → GSAP 0.85s → **700ms 内锁死**。
  后果：700ms 内的后续滚动被**直接丢弃**（不排队、也不接续），连滚三屏要等。
- 新实现：滚轮只更新 `target`，位置由 `springStep`（`a = -2ωv − ω²(x−target)`）逐帧追过去。
  `ω = 4/0.67 ≈ 6 rad/s`（临界阻尼稳定时间约 0.67s，取 `4/ω`；后续按实测手感调到 0.5s，见下文「手感修正」）。
- 收益（已实测）：间隔 150ms 连滚三次 → `01/03` 直达 `03/03`；运动途中反向会**保留速度改向**
  （位置 `-111 → -125.5（惯性冲过头）→ 一路回到 -100.008` 稳定）。

> 那个 `-125.5` 的冲过头不是缺陷，是保留速度的必然结果。想要无冲过头就得归零速度，
> 代价是打断时的顿挫 —— 这里选择保留速度。

### 缓动：`smootherstep` 取代 `expo.out`

`smootherstep(t) = t³(10 − 15t + 6t²)`，一阶与二阶导数在 t=0、t=1 处都为 0（**两端无速度突变**）。
`expo.out` 是"快出慢停、带冲劲"，与水滴玻璃的沉稳相反。欢迎页的光斑则保留 `elastic`（"水滴"该有的回弹）。

### 入场：波包取代等距 stagger

```
wavePacket(d) = 2.5·bell(d, 3.8) − 0.58·bell(d − 6, 3.5)      bell = 高斯钟形
```

波峰带肩膀、其后跟一段浅波谷 —— 相邻元素读的是**同一条曲线**，所以看起来是"一个连续表面被推动"，
而不是"各自错开的一堆独立 tween"。距离计算中行方向乘 1.6（屏幕横向更宽，让波前保持接近对角）。
二维波前换行后会回到波源附近，这是正确行为，**不能**改成"逐条延迟依次加长"。

### 背景光斑：互质周期叠加取代三套 CSS keyframes

- 旧实现：`drift-a 26s` / `pulse 9s` / `wave 20s` —— 单一周期，看久了能察觉在循环。
- 新实现：每个变体都是**两个互质周期叠加**（23/31、29/17、19/27、8/13、20/9、26/41…），
  相邻层错相 1.7 rad。**重复周期变成两者的最小公倍数**，实测 8s/13s/26s/40s 都不是周期。
- 幅度：以 `1vmin` 为单位，最宽漂移 0.37 单位；缩放下限 0.7（不得翻面）。
- 页面隐藏时停掉 rAF；`prefers-reduced-motion` 下不启动 rAF。

**否决**：让 CSS keyframes 承担这件事。理由：无法保证互质、无法在页面隐藏时停止、
也无法给相邻层做相位错开。

### 时间分档：进入慢、退出快

| 场景 | 进入 | 退出 |
|---|---|---|
| 面板 / 卡片 | 0.3s | 0.2s |
| 覆盖层 | 0.32s | 0.22s |
| 卡片入场 | 0.8s | — |
| 整屏切换 | 临界阻尼 ≈0.5s 稳定（ω=8） | 同 |

理由：退出时用户已经知道结果，等待时间纯属损耗。全部数值集中在 `src/motion/tokens.ts`。

### 手感修正：短程位移的曲线、菜单滑入、正文收尾（同日追加）

全部由实测反馈驱动：

1. **短程位移不再用 `smootherstep`**。它的两端速度归零，10% 时长只走 0.9% 的位移——
   用在整屏慢镜头很好，但按钮滑动 / 菜单拉出这类短程响应会「先卡住再猛冲」。
   新增 `easings.snappy = power3.out` 交给指示条，菜单拉出用 `easings.soft`（`power2.out`）。
   （实测指示条：76ms 已走完全程的 55%）
2. **菜单必须有真实位移**。只做 `height: 0 → auto` 时缓动是「看不出来」的——
   高度变化看起来只是「长出来」。所以内层补 16px 向上滑入（`MENU_SLIDE`），
   外层高度仍负责把分隔线及以下内容整体上抬。同时去掉菜单的玻璃面板与重复的
   「主题色 · 时段」行：面板 + 身份行 + 更多按钮叠了三层矩形，信息也重复。
3. **正文收尾**：`pageSettle` 0.67s → 0.5s（ω 6 → 8），并加亚像素吸附
   （`SNAP_EPSILON = 0.0005` 屏）。临界阻尼是指数收敛，最后零点几个百分点要拖很久，
   看着像「停不下来」；吸附让收尾干脆，而 0.05% 屏在任何屏幕上都不到一个像素，看不出跳变。
4. **元素级入场：流水式涌入（无过冲）**
   - 缩放：`scale 0.93 → 1`。
   - 位移：**方向跟随翻页** —— 新 section 从哪边进来，元素就从哪边流入
     （向下翻页从下方流入，向上翻页从上方流入），起始距离 `REVEAL_RISE = 640px`。
     幅度沿用波包（`waveAssign`），靠后的元素起始更近，所以**起始间距比最终间距更紧**（压缩）。
   - **手机模式（横向轴）改为左右流入**：那一档整屏是左右切换的，元素再上下流入就与容器错位；
     两者共用同一个 `dir`，且 to 同时把 `x`/`y` 归零，避免换轴时残留上一个轴的位移。
   - **退场必须单独做**：只给新页面播入场时，切走的那一页是被容器硬拖走的，自己没有任何动作，
     两边不对称。现在旧页面向**翻页去向**流走（位移方向取 `-dir`）、同步淡出，错开与曲线跟入场一致。
   **波包错开不能去掉**：它是「层次感」的来源。曾经为了「压缩又不重叠」把错开删掉、
   改成所有元素同时涌入，结果退化成整块平移，看不出幅度差异也没有被推着走的感觉 ——
   用户直接反馈「没有之前那种弹簧效果了」。恢复错开、方向跟随翻页后才是正解。
   **方向必须跟随翻页**：固定成一个方向时，反方向那次元素是「逆着容器」走的，读起来就是拉伸。
   这正是当时「从下向上拉伸、从上向下正常」的来源。
   **真正的坑：CSS `transform` 过渡会吃掉 GSAP 写入的起始值**。
   `.feature-card` 原本用 `transition: transform` 做悬停位移，GSAP 逐帧写入的
   `translateY` 被过渡拦住，卡片 y 恒为 0（标题组有值）—— 这才是上面 57 帧重叠的真因，
   坐标分配只是表象。修法：悬停位移改用独立的 `translate` 属性（与 `transform` 互不干扰）。
   **教训**：给「会做 transform 动画」的元素挂 CSS transform 过渡，一定会打架 ——
   入场目标元素尤其要避开。
   曲线用 `power2.out`：单向、不迟滞、尾段匀速收住 —— 像流水。
   **入场不再错开（取消了波包 stagger）**：位移远大于元素间距时，先动的元素会立刻追上
   并越过后面的元素。实测同一行两张卡片的间距一度变成 **-231px**。
   先动、后动两种顺序都试过，都会穿越 —— 要压缩又不要重叠，只能同时起步。
   （`waveAssign` 仍保留在 `motion/math.ts` 供后续使用，只是入场不再用它。）
   **分配收紧量按纵向比例，不按 index**：按 index 会让靠下元素的起始位置更靠上，
   与目标位置顺序相反，同样会穿越。
   **首次进入用四成幅度**（`FIRST_REVEAL_SCALE = 0.4`）：从登录页整体切过来时，
   足幅度的涌入叠在「换了页面」这件事上会显得特别猛；正文之间互切时才给满。
   **历史记录（都试过、都否决）**：
   - 加在整个 `.section__body` 上（整块被压 0.8%、后拖 0.8% 屏）：数值都对（过冲 17%），
     但看不出来 —— 翻页本身就是大位移，0.8% 的小形变被淹没；而且它按速度逐帧跟随，
     形变随速度一起衰减，等到判停时已所剩无几（实测过冲只剩 0.0001）。
   - `elastic` 过冲：位移只给 28px 时过冲仅 1.7px，观感全被缩放盖住；拉到 64px 过冲才可见，
     但那个「弹」正是后来要去掉的。
   结论：幅度要够大、加在**元素**上、且**不要过冲**。
5. **轴判定不能看 `scrollHeight`**：`useOverflowMode` 原用 `scrollHeight > clientHeight` 判断
   「内容装不下」，但**滚动区域会把 transform 造成的溢出算进去** —— 入场位移拉到 640px 后，
   `scrollHeight` 从 982 涨到 1313，于是被判成装不下、轴切成横向、三个 section 重新排列，
   表现为「页面在左右移动」。改为比对 `.section__body` 的 `offsetHeight`（布局尺寸，不含
   transform）加 section 的上下 padding。实测：翻页全程轴不变；把 section 压到 300px
   仍能正确切成横向，恢复后切回纵向。
   （该文件原注释写着「判定与当前模式无关、不会来回抖动」—— 这个前提是被本次位移打破的。）

> `4/ω` 只是「大体到位」的口径（此处约剩 9% 位移），**不等于**视觉停稳时间，后者要更久。

---

## 2026-09-11 · 主页面交互轴：装得下就纵向，装不下就横向

需求先后来了两条：先要求"任何尺寸下都不出现滚动条"，又追加"当会溢出时转为手机逻辑：
左右滑动切换，上下正文可滑动"。后一条其实更好——它把纠结消掉了。

- **装得下** → 纵向整屏切换（滚轮 / 上下键 / 上下滑），正文不滚动。设计稿的默认形态。
- **装不下** → 横向切换（左右滑 / 左右键 / 横向滚轮 / Shift+滚轮），正文纵向自己滚。

判定用 `section.scrollHeight > clientHeight`，任一页超出就整体切到横向模式。
两种模式下 `.section` 的宽高都等于视口，所以几何判定与当前模式无关，**不会来回抖动**。

### 实测

| 视口 | 内容溢出 | 模式 | 页面滚动条 |
|---|---|---|---|
| 3440×1440 / 2560×1080 / 1920×1080 | 0 | 纵向 | 无 |
| 1280×720 | 原 60px → **0**（流式留白修好） | 纵向 | 无 |
| 1024×768 | 功能页 101px | **横向** | 无 |
| 390×844 | 123px | 横向 | 无 |

一行值得记住：**1024×768 的溢出主因是宽度**（列数变少 → 卡片换行变高），所以"装不装得下"
同时取决于宽与高，不能只看高度——表格里 1280×720 反而装得下就是这个原因。

### 否决：整体等比缩放

最初写的是"装不下就把内容 `scale` 到刚好装下"（`--fit-scale` + ResizeObserver）。
能 work，但矮屏会把字缩到难读，而且与纵向/横向两轴是**两套重叠的机制**。
既然横向模式已能优雅承载滚动，缩放就是多余复杂度，已整体删除。

### 配套细节

- 横向模式的手势分工：`|deltaX| > |deltaY|` 才算切页意图，否则一律让给正文滚动（不 preventDefault）。
- 纵向模式保留"正文先滚完再翻页"的让位，但只在 `overflow-y: auto` 时生效。
- 两轴共用同一套临界阻尼弹簧，只是把 transform 从 `translate3d(0, ±N%, 0)` 换成 `translate3d(±N%, 0, 0)`。
- **滚动条只藏轨道、不禁止滚动**（`scrollbar-width: none` + `::-webkit-scrollbar`）。玻璃界面里一条粗滚动条
  非常破坏观感，而横向模式下正文本来就要滚。已验证：滚动条占位宽度 0，滚轮仍能把正文从 0 滚到 101。
  **例外**：后台宽表格 `.table-wrap` 保留滚动条——那里的滚动条是"还有更多列"的必要提示。

---

## 2026-09-11 · 主题色模型（校验脚本抓出的真缺陷）

### 文字色**必须离散**，不能参与插值

背景从浅到深、文字从深到浅，两者**同时连续插值必然在中途亮度相等**，那一瞬间对比度趋近于 0
（实测 19:00→22:00 区间中点对比度仅约 10，文字几乎不可读）。

这不是阈值没调好，是模型错了。修正：文字色改为按背景亮度**离散切换两档**，
阈值取亮度中点 `128` —— 这样最差时刻（背景正好落在阈值上）仍有半个亮度区间，
实测量差 **110.6**（出现在 02:17），远高于可用下限。一天只穿越阈值 **2 次**。

CSS 侧配套：`body` 的背景过渡仍是 4s（保证时段渐变平滑），但**文字色过渡压到 200ms**
—— 文字色是离散切换的，拖长会经过中灰而与背景糊在一起。

### 其他修正

- `fgMuted` 在锚点里是 `rgba()` 格式，却走了 hex 插值 → 产出 `#NaNNaNNaN`。现改为离散两档直接给出。
- 颜色统一**大写**：锚点色板是大写书写，插值结果必须能与之逐字符比对。

### 已固化的数值

| 不变量 | 实测 | 阈值 |
|---|---|---|
| 相邻 1 分钟最大通道差 | 2（05:02） | ≤ 3 |
| 凌晨 / 中午背景亮度 | 28.4 / 250.5 | 夜间须明显更暗 |
| 最差前景背景亮度差 | 110.6（02:17） | > 100 |
| 文字色一天切换次数 | 2 | ≤ 2 |

### 待决（留给你定）

**19:00 → 22:00 之间约 1.5 小时背景处于中灰**：20:49 实测 `#747686`（亮度 119）。
这是"晚上(浅) → 午夜(深)"线性插值的必然结果。若觉得这段发闷，可以在 **20:30** 加一个
已经转深的锚点，把入夜压缩到更短时间；代价是夜间变色的"渐变感"会减弱。
用 `reference/palette-review.html` 拖时间轴即可对比。

---

## 2026-09-11 · 临时：管理员密码固定为 `123`

本地模拟期间要求使用固定口令。做法与代价：

- `docker-compose.yml` 的 `ADMIN_PASSWORD` 默认值由「留空 → 随机生成」改为 `123`（仍可用环境变量覆盖）。
- 该变量**只在账号不存在时生效**，所以换口令的实际操作是：删掉 `users` 里的 admin 行 → 重启容器 → 冷启动重建。
- 已验证：`123` 登录 200，旧随机口令 401。
- **代价（务必记住）**：`123` 只有 3 位，不满足应用自身对注册用户的 ≥ 8 位要求 —— 管理员创建路径不做强度校验，
  这是一处已知的不一致。**对外部署前必须用环境变量覆盖成强口令**，且该默认值会随仓库公开。
- 正经做法是加一个改密接口，而不是靠删行重建。

---

## 2026-09-11 · 侧栏改成四个页面 + iOS 27 液态玻璃

### 侧栏：从「三个入口 + 页脚菜单」改成四个等权页面

原方案是三个页面入口，站名 / 身份 / 管理后台 / 退出登录挤在侧栏页脚，点「更多」从下方拉出面板。
侧栏收到 52px 后这条路走不通：四个汉字的面板在 52px 里排不下，而在流内撑开又会被
高度动画那层 `overflow: hidden` 连同横向一起裁掉（同时裁两个轴）。

改为：**「更多」升格成第四个页面**（站名、时段与身份、管理后台、退出登录），
四个按钮完全等权、共用同一个滑动指示条。因此删掉了整块弹出面板逻辑与分隔线。

按钮改成纯图标。这是被迫的：两个字 30px + 两侧呼吸已经贴住 52px 的边，再加入第四个就溢出。
代价是标签得换一条路可读 —— `aria-label` 给屏幕阅读器、`title` 给鼠标用户
（ux-guidelines 把「icon-only button without label」列为反模式）。

**已否决**：做成从侧栏右侧浮出的弹出层（`position: absolute` + 宽度 `max-content`）。
它确实能解决排字问题，但用户要的是第四个**页面**，不是第四个按钮展开菜单。

### 几何：让胶囊在**四周**都比面板大一圈

之前只有左右各溢出 4px。改动是四处联动：

| 量 | 原 | 现 | 说明 |
|---|---|---|---|
| 图标渲染尺寸 | 20px | **24px** | 面板宽度不动，图标放大 20% |
| 按钮 | 60×68 | 60×72 | 玻璃变高，仍是竖向胶囊（60 < 72） |
| 面板纵向内边距 | 12px | **6px** | 剩下的交给负外边距 |
| `.sidebar__nav` 纵向外边距 | 0 | **-10px** | `10 = 6 + 4` |

结果是面板 `52×304`（原 `52×320`，**变矮了**），而最上 / 最下的胶囊在左、右、上、下
**各溢出 4px** —— 四边均匀，读起来才是「一圈」。

**为何必须靠负外边距**：面板高 = 胶囊堆栈高 − 上下溢出量。
「按钮变高」与「面板变矮」这两个要求只能同时满足于把整列顶出面板。
实测 `overflow: {left:4, right:4, top:4, bottom:4}`。

### 液态玻璃：折射只能靠 SVG 位移滤镜

纯 `backdrop-filter: blur()` 做不到「对背景的形变」—— 它只会糊。真正的形变要靠
`feTurbulence` 生成低频噪声当位移图，再用 `feDisplacementMap` 挤 `SourceGraphic`；
在 `backdrop-filter` 里它就是**元素背后的画面**。

三个非显然的坑：

1. **顺序必须是「先挤后模糊」**。写成 `blur(18px) url(#…)` 等于没挤 —— 模糊已经把要挤的细节抹平了。
   所以带 `url()` 的那条把模糊降到 `10px`。
2. **必须写两条声明**：不带 `url()` 的在前、带 `url()` 的在后。后一条无效会被 CSS 丢弃，前一条保留。
   只写一条的话，不支持 `url()` 的浏览器上会连模糊一起失效，玻璃直接变全透明。
3. **噪声频率要低**（`0.012 0.022`）且先抹平（`stdDeviation 3`）。频率一高就是「磨砂颗粒」；
   不抹平则位移场是碎的，背景会被切成一格一格，而不是被「挤」。

性能上做了取舍：位移滤镜只挂在**面积小、又是视觉焦点**的侧栏面板（52×304）与选中胶囊上，
`.glass` 卡片仍只用纯模糊 —— 那几张卡片加起来几千平方像素，而背景光斑是逐帧在动的。

### 归档与新目录

旧版「更多」按钮的形变动画（三点 → 横线 → 转 90° → 箭头 ↑）在按钮变成页面入口后就没了调用方，
但那套手调坐标值得留，所以整体搬到 `src/design/morph.ts` 存档，
并由 `check-motion.mjs` 用 3 条断言锁住它的两条硬约束
（两个状态都绕视图中心、展开态相邻段必须重叠 —— 两者坏掉都很难用肉眼发现）。
收起态那三个点的形状被 `design/icons.ts` 的 `more` 图标复刻，所以第四个按钮的字形与旧版无缝衔接。

**新增目录 `frontend/src/design/`**：图标路径、形变几何、折射参数，以及一份口径说明（其 `README.md`）。
分工标准：**代码里能引用的参数写成 `.ts`，只能由 CSS 表达的写进 README**。

---

## 2026-09-11 · 修正：溢出量从 4px 提到 10px（取代上一条的几何部分）

上一条把溢出做成四边各 `4px`。实测**根本看不出来**，而且四边观感不一致 —— 原因是几何性的：

| 位置 | 面板轮廓在那里的宽度 | 胶囊比轮廓露出多少 |
|---|---|---|
| 腰部（面板的直线段） | 满宽 | 正好等于溢出量 |
| 顶端（半圆顶点） | **已经收窄到 0** | 远大于溢出量 |

所以溢出量小的时候，左右看着「没动」、上下看着「鼓得很凶」。`4px` 时这两个读数是 `4` 对 `16.7`。

改法是**面板从 52px 收窄到 40px**（列宽 76 → 64），胶囊尺寸与图标一个没动。
胶囊不变而面板每窄 2px，四周溢出就各多 1px —— 这是唯一「不动胶囊就能加大溢出」的手段；
放大胶囊会等比例拉高整列，侧栏会重新变长（那是上一轮刚压下去的）。

| 量 | 4px 版 | 10px 版 |
|---|---|---|
| 面板 | `52 × 304` | **`40 × 292`** |
| 胶囊 | `60 × 72` | `60 × 72`（不变） |
| 溢出 | 四边 `4` | **四边 `10`** |
| `.sidebar__nav` 纵向外边距 | `-10px` | `-16px`（`6 + 10`） |
| 顶端 vs 腰部的露出差 | `12.7px` | **`2.7px`** |
| 长宽比 | `5.8` | `7.3` |

实测 `overflow: {left:10, right:10, top:10, bottom:10}`。

**附带发现：面板高与内边距无关。** 推导 `面板高 = 2P + (堆栈 − 2(P+O)) = 堆栈 − 2O` ——
`padding-block` 加多少，负外边距就抵消多少。真正决定高度的是 `胶囊堆栈 − 上下溢出量×2`。
所以调内边距只能改「胶囊离面板边多远」，改不了面板长宽。

---

## 2026-09-11 · 整体放大 5% 与胶囊内部那条「框线」

### 三条约束之间的耦合（不能同时精确成立）

要求：胶囊宽锁定 `60`、面板等比 `+5%`、胶囊高 `+5%`、未选中图标 `+5%`、选中图标不变。
这几条不能同时精确成立，因为 `面板高 = 胶囊堆栈 − 上下溢出×2`：

- 面板宽 `+5%` ⇒ `42px`；胶囊宽锁 `60` ⇒ 溢出从 `10` **降到** `9`（`60 = 42 + 2×9`）
- 胶囊高 `75.6`（+5%）+ 间距 `8` + 溢出 `9` ⇒ 面板高 **`308.4`**，而非严格 +5% 的 `306.6`

差 `1.8px`（`0.6%`）。选择保住**四边溢出严格相等**（上一轮明确提过的诉求），
而不是面板高度那 0.6% —— 两者都不可见，但前者有明确来源。
`.sidebar__nav` 的负外边距同步成 `-15px`（`6` 内边距 + `9` 溢出）。

### 图标：未选中比选中大 5%

选中时弹出来的是胶囊，图标再跟着放大整块会显得笨重，所以拆成两套尺寸：
未选中 `25.2px`、选中 `24px`。两个值都在 `design/icons.ts`，
由 Sidebar 写成 CSS 变量交给样式表 —— 在 TSX 里按 `aria-current` 分支既啰嗦，
又会和 `aria-current` 的另一处用法（文字颜色）脱钩。

### 缺陷：胶囊内部有一条「边栏的框」

`backdrop-filter` 采样的是「元素背后已绘制的内容」。胶囊在面板之上，背后几乎整块就是面板，
而面板自己已经采过一次样了 —— 胶囊再采一次，采到的就是**面板的背景与它那圈 1px 描边**。
描边被模糊后在胶囊内部显成一条亮痕。而溢到面板外的 9px 采的是**未模糊**的原始背景，里外差出一层。

采样顺序绕不过去（把胶囊移出面板也没用：它照样在面板之上）。所以胶囊**去掉 `backdrop-filter`**，
改成纯手工叠光：白膜（两遍 `--glass-1/2`，等效不透明度 ≈ `0.39`）+ 亮边 + 顶部内高光
+ 底部内反光 + 内外投影。代价是溢出的那一圈不再是模糊的，换来一整块均匀的玻璃片。
全站现在只有侧栏面板一件带折射。

### 缺陷：指示条比按钮高 0.4px（两处整数陷阱）

1. `offsetWidth / offsetHeight / offsetLeft / offsetTop` 是**整数** API，而胶囊高 `75.6px`。
   改用 `getBoundingClientRect()` 的差值（安全前提：`.shell` / `.sidebar` / `.sidebar__nav` 上都没有 transform）。
2. GSAP 默认 `autoRound: true`，会把 px 值舍入到**整数** —— 上一步刚拿到的 `75.597` 又被舍回 `76`。
   显式传 `autoRound: false`。

修完实测四个 delta（位置 x/y、尺寸 w/h）全为 `0`。

---

## 2026-09-11 · 修正：图标的未选中/选中差异改用倍率（取代上一条的图标部分）

上一条用「两套绝对值」表达：未选中 `25.2px`、选中 `24px`（差 5%）。实测**看不出效果**，两个原因：

1. 5% 只有 **1.2px**。这一点尺寸差在任何屏幕上都不构成可辨识的层级。
2. 切换是**瞬时**的（只换了 `width`/`height`，没有过渡），所以连「变化」这个事实都感知不到。

改成倍率 + `transform`：

```css
.sidebar__btn svg {
  transform: scale(var(--icon-scale-idle, 1.2));   /* 未选中：×1.2 */
  transition: transform 240ms var(--ease-out);
}
.sidebar__btn[aria-current='true'] svg {
  transform: scale(1);                             /* 选中：×1 */
}
```

三点理由：

- **倍率而非两套绝对值**：基准尺寸（`ICON_SIZE = 24`）只写一次。
- **`transform: scale()` 而不是 `width`/`height`**：scale 是合成层变换，不触发布局，所以能平滑过渡；
  改宽高是布局动画，每帧重排，而且无法过渡出过程感。
- **差异拉到 20%**：未选中 `28.8px`、选中 `24px`，差 `4.8px`，一眼可辨。

过渡 `240ms` 比指示条的 `0.42s` 短：图标是局部反馈，先到位，整条指示条随后跟上 —— 层级分明。
实测 `matrix(1.2, …)` / `matrix(1, …)`，`idleOverActive = 0.2`。

---

## 2026-09-11 · 侧栏尺寸改成「总倍率 × 基准值」

### 问题：逐个数手算，已经算歪了

侧栏尺寸被连续微调了四轮：`52` → `40`（加大溢出）→ `42`（+5%）→ 本次。每一轮都是把上一轮的
数值乘一个系数再取整，于是基准值里混进了 `42`、`308.4`、`75.6`、`9`、`8` 这类「历史沉积物」，
而且**「等比例」在取整后只是近似**（上一轮就有 `0.6%` 的偏差）。

改成：

```css
/* .shell */
--sidebar-scale: calc(48 / 42);
```

所有侧栏尺寸写成 `calc(基准值 * var(--sidebar-scale))`，**基准值就是倍率 1 时的那一版**。
于是「整体等比放大」变成改一个数，不近似、也不会漏掉某处。写成 `calc(48 / 42)` 而不是小数，
是为了让「面板宽 42 → 48（左右各 +3px）」可追溯。

列宽也跟着算出来：`calc(24px + 42px * var(--sidebar-scale))`。

### 本次参数

倍率 `48/42 ≈ 1.1429`：

| 量 | 上一版 | 现值 | 倍率校验 |
|---|---|---|---|
| 面板 | `42 × 308.4` | **`48 × 352.4`** | 宽高都 `+14.28%` |
| 胶囊 | `60 × 75.6` | `68.55 × 86.38` | `+14.28%` |
| 间距 | `8` | `9.13` | `+14.28%` |
| 溢出（四边） | `9` | `10.28` | `+14.28%` |
| 长宽比 | `7.342` | `7.342` | **不变量** |
| 图标 | `24 / 28.8` | `24 / 28.8` | **刻意不变** |

### 图标不参与缩放

要求里明确「当前图标大小不变」。图标大小是单独定的设计参数（`design/icons.ts`），
与侧栏几何解耦 —— 所以 `.sidebar__btn svg` 的尺寸**不乘** `--sidebar-scale`。
（本轮一开始我给它乘上了，随即被要求撤掉。）

---

## 2026-09-11 · 侧栏的壳剥成独立一层，用胶囊形状挖洞

### 问题

胶囊比面板大一圈（四边各溢出 10.28px）。面板的壳（1px 描边 + 顶部内高光 + 底部内反光）
原本画在面板本体上，于是胶囊跨过面板轮廓时，**那圈框线会从胶囊内部穿过去**，
看着像「胶囊里套了个边栏的框」。

### 先试错掉的思路

「在描边上抠掉胶囊覆盖的那一段」—— 即求面板轮廓与胶囊轮廓的**交点**，只画交点之外的部分。
几何上是通的，但落地很难看：

- 面板轮廓在上/下端点处是**半圆弧**，胶囊顶端是**半椭圆弧**（宽高不等，`border-radius: 999px`
  被 clamp 成 `rx = 宽/2`、`ry = 高/2`），求交要解方程组。
- 而且结果**对首尾两个按钮与中间两个按钮不一样**：首尾时整个半圆都落在胶囊内，**无交点**。
- 用水平条带近似又会多挖：胶囊从顶点往下要 12.35px 才覆盖到面板全宽，
  而面板的竖边在那之前就被切掉了，会露出一个缺口。

### 采用的方案（按用户思路）

**面板本体只留背景与折射，另起一层 `.sidebar__edge` 专画壳，
再用一个与胶囊同形、同位的「洞」把它挖掉。** 不求交点 —— **形状本身就是答案**。

```css
.sidebar__edge {
  --hole: url("data:image/svg+xml,…<rect rx='50%' ry='50%'/>…");
  mask-image: linear-gradient(#000 0 0), var(--hole);
  mask-size: 100% 100%, var(--cut-w) var(--cut-h);
  mask-position: 0 0, var(--cut-x) var(--cut-y);
  mask-composite: exclude;      /* 全层 减 洞 */
  -webkit-mask-composite: xor;
}
```

几个关键点：

- **洞就是胶囊本身**：`rx`/`ry` 都是 `50%` 的矩形，拉伸到胶囊尺寸后正好是胶囊。
  `preserveAspectRatio='none'` 不能省 —— 没有它矩形会等比缩放、变成圆。
- **`--cut-*` 由 GSAP 与指示条同一条曲线、同一时刻写入**，否则洞跟不上胶囊，
  滑动途中会露出半截断口。用一个普通对象 tween + `onUpdate` 写 CSS 变量。
  ⚠️ 那个 tween 里**不能传 `autoRound`** —— 它是 CSSPlugin 专属属性，
  对普通 JS 对象 GSAP 会报 `Invalid property ... Missing plugin?`。
- **默认 `--cut-w/h` 为 `0`**（洞不存在）⇒ 壳完整可见，不会首帧闪一下。
- **壳层只在桌面出现**：移动端侧栏是顶部横条，胶囊不溢出，没有可切的地方。
- **投影与光晕留在面板本体上**：它们会「透」进胶囊里，但那是对的 ——
  投影本来就落在胶囊下方。切掉的只是硬描边。
  顺带删掉了 `0 0 0 1px rgba(255,255,255,0.14)` 那条外圈白边（它在元素**外部**，
  没法跟着壳层被裁掉，而它和 `inset` 描边几乎重合，去掉无感）。

### 验证

`mask-composite: exclude` 与 `-webkit-mask-composite: xor` 均被浏览器支持；
`mask-size` 解析为 `100% 100%, 68.5653px 86.392px`（= 胶囊实际尺寸），
`mask-position` 四个页面依次为 `-10.2841px` / `85.2415px` / `180.767px` / `276.293px`；
洞与胶囊的位置偏差 `dx = dy = 0`；控制台零警告。

---

## 2026-09-11 · 边栏本体去掉白色底膜

现象：边栏整体像铺了一层淡白底，不是纯玻璃。

根因不在透镜那几层，而在 `.glass`：它是**卡片**的玻璃类，自带一层
`linear-gradient(150deg, --glass-1, --glass-2)` —— 实测夜间是
`rgba(255,255,255,0.106) → 0.06`。面板一直挂着这个类，所以一直有这层白，
只是以前壳上的内高光 / 内反光（现已搬到 `.sidebar__edge`）盖着，不明显。

修法：桌面端 `.sidebar { background: none }`。

理由：面板的体量应该**全部**来自「折射 + 壳上的高光 + 外投影」。
再盖一层白，背景就被糊在下面了 —— 看着是一块发白的板，而不是玻璃。

保留项与衍生效果：

- 折射、外投影、主题色光晕全部保留。
- 壳仍在 `.sidebar__edge` 上，仍然被胶囊挖洞。
- **移动端保留底膜**：顶部横条又窄又扁，没有折射的余地，需要一点底色才有体量。
- 去掉底膜后，胶囊那层「白膜加倍」的对比才立得起来（面板 `0` vs 胶囊约 `20%`）。

实测：`backgroundImage: none`、`backgroundColor: rgba(0,0,0,0)`；
边栏内避开胶囊的一点的层栈里，**只有 BODY 的背景有颜色**。

---

## 2026-09-11 · 光标光晕改为跟随「壳」

现象：鼠标光晕描的是面板**完整的**胶囊轮廓，而那条边已经被胶囊按钮盖住了 ——
描的是一条看不见的线。

修法：光晕样式不变，只换目标 —— 菜单里加 `.sidebar__edge::after`，
同时桌面端把 `.sidebar::after` 关掉（`content: none`）。
壳层已经被胶囊挖了洞，光晕挂在它上面就自动沿着**真正可见的边**走。

两个细节：

- **不用改 `useCursorGlow`**：`--glow` / `--glow-x` / `--glow-y` 写在 `.sidebar` 上，
  壳作为子元素直接**继承**；两者 `inset` 都是 `0`、尺寸完全相同，
  所以百分比坐标含义也一致（实测两边都是 `50.0% / 20.0%`）。
- **源顺序陷阱**：`.sidebar::after { content: none }` 必须写在那组 `::after` 规则**之后**。
  两条选择器特性相同（`(0,1,1)`），靠源顺序决胜；写在前面会被默默覆盖且不报错。

实测：`.sidebar::after` 的 `content` 为 `none`，`.sidebar__edge::after` 为 `""`，
`--glow` 已继承，两者盒子完全重合。

---

## 2026-09-11 · 面板去掉 `saturate`（它才是「有色」的真正来源）

删掉白膜之后面板依旧「有颜色」。剩下的来源是 `backdrop-filter` 里的 `saturate(190%)`。

关键区别：**`saturate` 不像模糊那样只把颜色平均化，而是直接把背景的饱和度拉高。**
深色背景下 `rgb(29,30,47)` 这种灰蓝会被推成明显的紫蓝，整条面板看上去就「自带一层底色」。

修法：面板的两条 `backdrop-filter`（回退 + 增强）**都去掉 `saturate`**，只留形变与虚化。

修完面板内只剩三类画层，且**没有一处引入色偏**：

| 画层 | 颜色贡献 |
|---|---|
| `.sidebar` | `backdrop` = 位移 + 模糊，不改色 |
| `.sidebar__edge` | 三条 inset 全是白色（壳） |
| `.sidebar__pill` | 白膜 + 白 inset，只在选中胶囊处 |

实测：`backdropFilter` = `url("#liquid-glass") blur(10px)`，
面板白膜等效不透明度 `0`。

**仍在外部的彩色项**：`0 0 46px -6px rgba(176,153,251,0.14)` 那圈主题色柔光。
它围绕在面板**外部**，但视觉上贴着边栏，容易被当成边栏的一部分 ——
如果仍觉得有颜色，下一个该动的是它。

（注：`blur` 本身仍会让面板区域与周围不同 —— 那是模糊，不是上色，也是玻璃的根本。）

---

## 2026-09-11 · 面板收敛到「完全隐形」（一条被误导的排查链）

用户连报三次，我前两次都修在了错的地方。完整记下来，因为它值一个教训。

| 反馈 | 我的动作 | 是否命中 |
|---|---|---|
| 「边栏本身有底色，而非纯透明」 | 删 `.sidebar` 的 `background`（`.glass` 的 10% 白膜） | ✅ 确实是一层底色 |
| 「仍有颜色」 | 删 `backdrop-filter` 里的 `saturate(190%)` | ❌ 偏了 |
| 「仍有颜色，**而且更深了**」 | 摘掉 `url(#liquid-glass)`、删主题色柔光 | ❌ 仍偏 |
| 「是边栏**上下半圆有小小的白边**」 | 删壳上的三条静态 inset | ✅ **命中** |

### 真正的元凶

`.sidebar__edge` 上的三条 inset：

```css
inset 0 0 0 1px rgba(255,255,255,.62)            /* 描边 */
inset 0 1px 0 rgba(255,255,255,.85)              /* 顶部内高光 */
inset 0 -20px 42px -30px rgba(255,255,255,.75)   /* 底部内反光 */
```

它们沿面板轮廓画一圈，而**面板中间整段被胶囊挖掉了洞**（胶囊比面板大一圈，
四个胶囊几乎铺满面板高度）—— 于是只在**上下两端的半圆处各剩一小段白弧**：
细（1px）、碎、又孤零零的，看着就是「边栏上下半圆有一圈小白边」。

### 教训

- **「颜色」「更深」「底色」这类词太粗，会把我引到错的方向。** 前两次我都在查
  「有没有色相」，而实际是**一段残留的白色描边**。定位到「在哪一处」（上下半圆、
  还是整圈、还是内部）比任何属性审计都快。
- 我试过用像素采样来自证（截图 → canvas 读像素），连试 5 次都没走通：
  这个环境的 `page.screenshot` 返回的是**已被 UTF-8 解码破坏的字节串**
  （PNG 头 `0x89` 变成了 `U+FFFD`），且 `Buffer`/`btoa` 都不可用。
  另外它的 clip **越界会直接报错**（面板距视口左边只有 24px，取不到 -46）——
  这大概就是「这个窗口截图是空白」的真相。

### 现在的形态

面板：`background: none` / `backdrop-filter: none` / `box-shadow: none`，不画任何像素。
壳：也不画任何像素，只作为**光标光晕的载体**存在（光晕被同一个洞挖掉）。

⚠️ `box-shadow` 必须写 `none` 而**不能整条删掉** —— 删掉会退回 `.glass` 的
`inset 0 1px 0 rgba(255,255,255,0.55)`，那条又会在顶部添一道白边。
（这一轮还踩了一次**源顺序**：我把 `none` 写在前面，被后面旧的 `blur`/`box-shadow`
声明覆盖了，实测才发现。）

实测：面板与壳的「自身绘制」清单均为空；壳的 `mask` 仍在工作
（`xor, xor` / `68.5653px 86.392px` / `-10.2841px -10.2841px`）；胶囊照旧。

---

## 2026-09-11 · 面板加宽 20px，并把「宽度」与「倍率」解耦

要求：「图标与胶囊尺寸都不变，只把边栏加宽 20px」。
理由说得很清楚：**目前边栏和图标太近了** —— 图标和按钮的大小都合适，
所以只能动边栏。

### 先量化「太近」

| 距离 | 改前 | 改后 |
|---|---|---|
| 面板边缘 → 图标 | `12px` | **`22.29px`** |
| 胶囊边缘 → 图标 | `22.29px` | `22.29px` |
| 差 | `10.29` | **`0`** |

图标在面板里居中、也在胶囊里居中 —— 所以只要**面板宽 ≈ 胶囊宽**，这两个数就相等。
这就是用户说的「边栏和按钮左右边距相同」。

### 为什么不改倍率

胶囊宽写的是 `calc(100% + 18px * var(--sidebar-scale))` —— **挂在面板宽度上**。
面板一加宽，胶囊就跟着加宽，两个边距同时变大但**永远相等**，
问题只是被放大而不是被解决（而且违背「按钮尺寸不变」）。

所以拆成两个参数：

```css
--sidebar-scale: calc(48 / 42);       /* 胶囊 / 间距 / 溢出 / 面板高度 */
--sidebar-width: calc(48px + 20px);   /* 面板实宽，独立 */
```

并把胶囊宽改成**绝对值** `calc(60px * var(--sidebar-scale))`，居中交给
`.sidebar__nav` 的 `justify-items: center`。

### 放弃的约束

面板 `68` / 胶囊 `68.57` —— 两者几乎等宽，所以**左右方向的「溢出」已经归零**
（差 `0.57px`）。这主动放弃了早先那条「四边溢出必须相等」：
上下仍各溢出 `10.28px`，四边不再相等。换来的是上面那条更硬的**等距**判据。

### 实测

面板 `68.00 × 352.40`（宽 +20px、高不变）；胶囊 `68.55 × 86.38`（不变）；
图标 `23.99`（不变）；两个边距均为 `22.29`，**差为 0**；列宽 `91.99px`。

---

## 2026-09-11 · 面板收窄到 58px，并修掉「胶囊不居中」的真 bug

### 收窄

上一版把面板加到 `68px`，结果**和胶囊（`68.57px`）等宽了**，两者糊成一片，
读不出「一列按钮浮在一条轨道上」的结构。

取中间值 **`58px`**：

| 面板宽 | 面板边缘 → 图标 | 胶囊边缘 → 图标 |
|---|---|---|
| `48px` | `12px`（太近） | `22.29px` |
| `68px` | `22.29px`（等宽） | `22.29px` |
| **`58px`** | **`17px`** | `22.29px` |

### 真 bug：胶囊是「左对齐 + 右侧溢出」，不是居中

改宽后面板确实变了，但「面板边缘到图标」**仍是 `22.29`**（应为 17）。
一算就露馅：图标中心落在 `34.29 = 68.55 / 2` —— 胶囊从 `nav` 左边缘开始，
而不是居中。这也是上一版「两个边距差 0」的原因 —— **那是左对齐碰巧造成的假象**。

根因：上一轮把胶囊宽从 `calc(100% + 18px * scale)` 换成绝对值 `calc(60px * scale)` 时，
顺手删掉了负责居中的 `margin-inline` 负值，改为依赖 `.sidebar__nav` 的 `justify-items: center`。
但那个 grid **没有定义列**，隐式列宽是 `auto` —— 比容器宽的胶囊会把它撑开，居中就失效了。

### 第二个坑：`1fr` 锁不住列宽

先补了 `grid-template-columns: 1fr` —— **没用**。实测 `nav` 列宽仍是 `68.57px`。
因为 `1fr` 的最小值是 `auto`，内容照样把它撑开。

正确写法是 **`minmax(0, 1fr)`** —— 显式允许收缩到 0，列宽才会等于容器宽。

### 实测（修完）

面板 `58.00 × 352.40`；胶囊 `68.55 × 86.38`，**左右各溢出 5.28px（对称）**；
图标 `23.99 / 28.79`（不变）；面板边缘到图标 `17`、胶囊边缘到图标 `22.29`；
指示条与按钮的位置/尺寸偏差全为 `0`；`nav` 列宽 `58.0`。

---

## 2026-09-11 · 光标光晕与卡片边缘错位（包含块陷阱）

现象：正文卡片上的光标光环比卡片实际边缘**内缩**，看着是错位的。

### 根因

**绝对定位伪元素的包含块是「最近的 positioned 祖先的 padding box」，不是 border box。**

卡片都挂着 `.glass`，带 `1px` 边框，所以 `inset: 0` 对齐的是 padding box ——
光环比卡片**可见的**边缘每边内缩 `1px`。

实测 18 个元素全部错位，量值完全一致：

```
border      : 0.909091px
afterInset  : 0px
左/上错位    : 0.91px
宽差 / 高差  : 1.82px
```

`border-radius: inherit` 也一起错：它继承的是**外圆角**（`18px` / `999px`），
却被画在缩小后的盒子上 —— 所以圆角处的偏差比直边更大。

### 修法

定位必须分两套写（这两类元素的 border 状态不同，不能共用一条 `inset`）：

```css
/* 有 1px 边框：负 1px 拉回 border box */
.feature-card::after, .stat::after, .contact__card::after,
.more__card::after, .btn::after, .sidebar::after { inset: -1px; }

/* 壳没有边框，padding box 就是可见边缘 */
.sidebar__edge::after { inset: 0; }
```

共用部分（尺寸、圆角、渐变、mask 抠环、过渡）仍写在一条规则里，只把 `inset` 拆出来。

### 残余与原因

修完残余 `0.18px`（每边 `0.09px`），来源查清了：本机 `devicePixelRatio = 1.1`
（110% 缩放），Chrome 把 `1px` 边框**对齐成 1 个设备像素** = `0.909px` CSS，
而 `inset: -1px` 按名义值走，于是过冲 `0.09px`。

远小于一个设备像素，不处理。要完全消除只能把 `.glass` 的边框换成 inset 描边
（那样 padding box 就等于 border box），但那要连带改三处 hover 的 `box-shadow`，不划算。

---

## 2026-09-11 · 假边栏的边与胶囊「交叉」

现象：鼠标扫过时，假边栏（`.sidebar__edge`）上的光环与胶囊按钮有一点交叉。

### 先排除的：几何是准的

实测洞与胶囊**完全对齐**：`dx = dy = 0`、尺寸差 `0.01px`。
洞的形状与胶囊同源（`rx/ry` 都是 `50%` 的矩形拉伸到胶囊尺寸），
`border-radius` 与 `mask` 的椭圆半轴也都是 `34.28 / 43.19`。所以不是错位。

### 真正的原因：两条轮廓形状不同，交界处只有抗锯齿

- 光环沿**面板**轮廓走 —— 面板是 `58 × 352.4` 的胶囊，半轴 `29 / 176.2`，
  在胶囊附近几乎是一条**接近竖直的扁弧**；
- 胶囊自己的轮廓是 `68.6 × 86.4`，半轴 `34.3 / 43.2`，是一条**明显弯曲的弧**。

两条弧形状不同，所以光环被洞切断的位置**正好落在胶囊的弧线上**，
交界处只有零点几像素的抗锯齿过渡 —— 看起来就是「边和按钮交叉在一起」。

### 修法

**洞比胶囊每边外扩 1px**：

```css
--hole-pad: 1px;
mask-size: 100% 100%,
           calc(var(--cut-w, -2px) + var(--hole-pad) * 2)
           calc(var(--cut-h, -2px) + var(--hole-pad) * 2);
mask-position: 0 0,
               calc(var(--cut-x, 1px) - var(--hole-pad))
               calc(var(--cut-y, 1px) - var(--hole-pad));
```

光环因此**在离开胶囊边缘之前就被切断**，两者之间留一条 1px 的干净间隙
（肉眼不可见），交界不再糊在一起。默认值取 `-2px` 是为了让未初始化时
洞的尺寸正好为 `0`（不挖），首帧不会闪。

实测：洞 `70.57 × 88.39` vs 胶囊 `68.55 × 86.38`，每边恰好多 `1px`。

### 另一个候选（尚未处理）

胶囊自己带一圈 `0 0 22px -4px var(--accent-soft)` 的淡紫辉光，22px 模糊会向
胶囊外扩散约 18px，**必然跨过假边栏的光环**。如果看到的「交叉」是**紫光**
而不是白边，那元凶是它 —— 去掉或换成中性色即可。

---

## 授权边界与禁止项

- **不改** agent 配置与核心配置文件。
- 管理后台放在 `/admin` 独立路径，**不占用** `design.md` 2.5 规定的侧栏入口（入口是四个页面的「更多」，后台只是它里面的一個链接）。
- 全站文案必须来自 `src/content/site.ts`，**不得**硬编码进组件。
- 视觉语言以 `design.md` 2.1（浅色系玻璃）为准；RhineLabUI 只借**机制**（运动数学、对照页、
  决策日志形态），不借**皮肤**（近黑暖灰、终端等宽排字、扫描线）。
- 未提即不做。

---

## 变更后必须跑的三件事

```bash
cd frontend && npm run check     # 内容 7 / 运动数学 12 / 时段主题 6，共 25 项断言（build 前自动执行）
cd frontend && npm run build     # tsc --noEmit + vite build
cd backend  && go test ./tests/... -count=1   # 24 项，含 HTTP 集成测试
```

改完动效或配色，另外打开 `http://localhost:5173/reference/palette-review.html` 看实时读数
（该页只在开发服务器存在，不进入生产构建）。
