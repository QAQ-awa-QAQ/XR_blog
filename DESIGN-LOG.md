# 决策日志

按日期追加的**决策与依据**记录。三条规矩：

1. **每条都带数值和理由** —— 三个月后回看，能知道当初为什么这么定，而不是重新想一遍。
2. **否决的方案也要写** —— 试过什么、为什么不用。撤回的记录比采纳的记录更省事。
3. **只追加，不修改历史条目** —— 改变主意就新开一条，并注明它取代了哪条。

需求本身见 `design.md`（唯一需求源），执行方案见 `plan.md`。

---

## 2026-09-11 · 需求澄清（18 项）

开工前逐条确认，避免"猜需求"。要点与理由：

| 项 | 决策 | 影响 |
|---|---|---|
| 前端 | React 19 + Vite + TS + GSAP | 不装 router：切换靠状态，`/admin` 用 pathname 判断，省一个依赖 |
| 后端 | Go + Gin + GORM + SQLite（纯 Go driver） | 免 CGO，容器内静态构建 |
| 容器 | 单容器内 Nginx + Go 双进程 | 前端产物由 Nginx 直接托管，无需跨容器 |
| 注册 | 开放注册 **+ 邀请码必填** + 分角色 | 邀请码一次性、7 天有效；admin 由冷启动创建，因此不存在"要先有码才能建码"的死锁 |
| 会话 | Cookie 存 session id，服务端 Redis 会话表 | 而非无状态 JWT —— 便于即时踢下线 |
| 密码 | argon2id（m=64MB, t=1, p=4） | 账号不存在时也跑一次哈希，避免用响应时间枚举账号 |
| 封禁 | 首次 24h、再犯永久 | 因此 `offense_count` **永不清除**，否则重启后无法区分首次与再犯 |
| 邀请码 | 16 位、去掉易混字符（0/O/1/I/l） | 便于口头或手抄传递 |

**否决**：暗色模式切换、评论、搜索、订阅、i18n、后台数据看板。理由：`design.md` 未提，未提即不做。

---

## 2026-09-11 · 安全规则的两处模型修正

### 1. 4.1 与 4.2 原本互斥

4.1 写"冷却期间不接收该 IP 的任何请求"，4.2 又要求"20 秒内 20 次"。冷却期若完全不接收、不计数，**20 次阈值永远达不到**，4.2 是一条死规则。

修正：冷却期返回 429，但**仍然计数**。这样两条规则才可共存。

### 2. 闸门与计数的职责边界

Nginx `auth_request` 只把 401/403 当作拒绝，返回 429 会被当成 500。所以闸门统一返回 **403 + `X-Block: banned|cooldown`**，由 `@blocked` 决定跳向哪一页。

**实测踩到的坑**：闸门最初把冷期间的 API 请求也一起 302 走了，20 次请求里只有前 3 次到达 Go，计数停在 3 —— 4.2 在真实部署中彻底失效。修正：`/api/auth/login` 与 `/api/auth/register` **豁免闸门**，改由 Go 的 `ipguard` 判定。

**禁止**在 Nginx 上加 `limit_req`：Nginx 若先返回 503，Go 同样数不到 20 次。阈值判定只放在 Go 里。

> 教训：Go 单测 24 项全绿，也测不出这个缺陷 —— 它跨了 Nginx 与 Go 两层。跨层行为必须真机验证。

---

## 2026-09-11 · 部署三处修正

| 问题 | 修正 | 理由 |
|---|---|---|
| 镜像构建失败 | `ARG GOPROXY` 必须声明在 `go mod download` **之前** | `ARG` 只对声明之后的 `RUN` 生效；容器内直连 `proxy.golang.org` 是 connection refused |
| 重定向丢端口 | `absolute_redirect off;` | 容器内监听 80、对外映射到其他端口时，默认生成的绝对地址会丢掉端口号 |
| 宿主机改库不生效 | 改完必须 `docker compose restart app` | Docker Desktop(Windows) 的文件共享层有缓存，容器看不到宿主侧的写入；排查时一度误判为封禁逻辑有错 |

---

## 2026-09-11 · 视觉与运动基线

视觉基线来自 `ui-ux-pro-max` 生成的 `design-system/xr-personal-blog/MASTER.md`（Liquid Glass / Inter / blur 18px / 玻璃膜 0.15）。

### 关键改动：整屏切换改用**临界阻尼弹簧**，废弃"事件 + 时间锁"

- 旧实现：滚轮事件 → `goTo(index±1)` → GSAP 0.85s → **700ms 内锁死**。
  后果：700ms 内的后续滚动被**直接丢弃**（不排队、也不接续），连滚三屏要等。
- 新实现：滚轮只更新 `target`，位置由 `springStep`（`a = -2ωv − ω²(x−target)`）逐帧追过去。
  `ω = 4/0.67 ≈ 6 rad/s`（临界阻尼稳定时间约 0.67s，取 `4/ω`；后续按实测手感调到 0.5s，见下文「手感修正」）。
- 收益（已实测）：间隔 150ms 连滚三次 → `01/03` 直达 `03/03`；运动途中反向会**保留速度改向**
  （位置 `-111 → -125.5（惯性冲过头）→ 一路回到 -100.008` 稳定）。

> 那个 `-125.5` 的冲过头不是缺陷，是保留速度的必然结果。想要无冲过头就得归零速度，
> 代价是打断时的顿挫 —— 这里选择保留速度。

### 缓动：`smootherstep` 取代 `expo.out`

`smootherstep(t) = t³(10 − 15t + 6t²)`，一阶与二阶导数在 t=0、t=1 处都为 0（**两端无速度突变**）。
`expo.out` 是"快出慢停、带冲劲"，与水滴玻璃的沉稳相反。欢迎页的光斑则保留 `elastic`（"水滴"该有的回弹）。

### 入场：波包取代等距 stagger

```
wavePacket(d) = 2.5·bell(d, 3.8) − 0.58·bell(d − 6, 3.5)      bell = 高斯钟形
```

波峰带肩膀、其后跟一段浅波谷 —— 相邻元素读的是**同一条曲线**，所以看起来是"一个连续表面被推动"，
而不是"各自错开的一堆独立 tween"。距离计算中行方向乘 1.6（屏幕横向更宽，让波前保持接近对角）。
二维波前换行后会回到波源附近，这是正确行为，**不能**改成"逐条延迟依次加长"。

### 背景光斑：互质周期叠加取代三套 CSS keyframes

- 旧实现：`drift-a 26s` / `pulse 9s` / `wave 20s` —— 单一周期，看久了能察觉在循环。
- 新实现：每个变体都是**两个互质周期叠加**（23/31、29/17、19/27、8/13、20/9、26/41…），
  相邻层错相 1.7 rad。**重复周期变成两者的最小公倍数**，实测 8s/13s/26s/40s 都不是周期。
- 幅度：以 `1vmin` 为单位，最宽漂移 0.37 单位；缩放下限 0.7（不得翻面）。
- 页面隐藏时停掉 rAF；`prefers-reduced-motion` 下不启动 rAF。

**否决**：让 CSS keyframes 承担这件事。理由：无法保证互质、无法在页面隐藏时停止、
也无法给相邻层做相位错开。

### 时间分档：进入慢、退出快

| 场景 | 进入 | 退出 |
|---|---|---|
| 面板 / 卡片 | 0.3s | 0.2s |
| 覆盖层 | 0.32s | 0.22s |
| 卡片入场 | 0.8s | — |
| 整屏切换 | 临界阻尼 ≈0.5s 稳定（ω=8） | 同 |

理由：退出时用户已经知道结果，等待时间纯属损耗。全部数值集中在 `src/motion/tokens.ts`。

### 手感修正：短程位移的曲线、菜单滑入、正文收尾（同日追加）

全部由实测反馈驱动：

1. **短程位移不再用 `smootherstep`**。它的两端速度归零，10% 时长只走 0.9% 的位移——
   用在整屏慢镜头很好，但按钮滑动 / 菜单拉出这类短程响应会「先卡住再猛冲」。
   新增 `easings.snappy = power3.out` 交给指示条，菜单拉出用 `easings.soft`（`power2.out`）。
   （实测指示条：76ms 已走完全程的 55%）
2. **菜单必须有真实位移**。只做 `height: 0 → auto` 时缓动是「看不出来」的——
   高度变化看起来只是「长出来」。所以内层补 16px 向上滑入（`MENU_SLIDE`），
   外层高度仍负责把分隔线及以下内容整体上抬。同时去掉菜单的玻璃面板与重复的
   「主题色 · 时段」行：面板 + 身份行 + 更多按钮叠了三层矩形，信息也重复。
3. **正文收尾**：`pageSettle` 0.67s → 0.5s（ω 6 → 8），并加亚像素吸附
   （`SNAP_EPSILON = 0.0005` 屏）。临界阻尼是指数收敛，最后零点几个百分点要拖很久，
   看着像「停不下来」；吸附让收尾干脆，而 0.05% 屏在任何屏幕上都不到一个像素，看不出跳变。
4. **元素级入场：流水式涌入（无过冲）**
   - 缩放：`scale 0.93 → 1`。
   - 位移：**方向跟随翻页** —— 新 section 从哪边进来，元素就从哪边流入
     （向下翻页从下方流入，向上翻页从上方流入），起始距离 `REVEAL_RISE = 640px`。
     幅度沿用波包（`waveAssign`），靠后的元素起始更近，所以**起始间距比最终间距更紧**（压缩）。
   **波包错开不能去掉**：它是「层次感」的来源。曾经为了「压缩又不重叠」把错开删掉、
   改成所有元素同时涌入，结果退化成整块平移，看不出幅度差异也没有被推着走的感觉 ——
   用户直接反馈「没有之前那种弹簧效果了」。恢复错开、方向跟随翻页后才是正解。
   **方向必须跟随翻页**：固定成一个方向时，反方向那次元素是「逆着容器」走的，读起来就是拉伸。
   这正是当时「从下向上拉伸、从上向下正常」的来源。
   **真正的坑：CSS `transform` 过渡会吃掉 GSAP 写入的起始值**。
   `.feature-card` 原本用 `transition: transform` 做悬停位移，GSAP 逐帧写入的
   `translateY` 被过渡拦住，卡片 y 恒为 0（标题组有值）—— 这才是上面 57 帧重叠的真因，
   坐标分配只是表象。修法：悬停位移改用独立的 `translate` 属性（与 `transform` 互不干扰）。
   **教训**：给「会做 transform 动画」的元素挂 CSS transform 过渡，一定会打架 ——
   入场目标元素尤其要避开。
   曲线用 `power2.out`：单向、不迟滞、尾段匀速收住 —— 像流水。
   **入场不再错开（取消了波包 stagger）**：位移远大于元素间距时，先动的元素会立刻追上
   并越过后面的元素。实测同一行两张卡片的间距一度变成 **-231px**。
   先动、后动两种顺序都试过，都会穿越 —— 要压缩又不要重叠，只能同时起步。
   （`waveAssign` 仍保留在 `motion/math.ts` 供后续使用，只是入场不再用它。）
   **分配收紧量按纵向比例，不按 index**：按 index 会让靠下元素的起始位置更靠上，
   与目标位置顺序相反，同样会穿越。
   **首次进入用四成幅度**（`FIRST_REVEAL_SCALE = 0.4`）：从登录页整体切过来时，
   足幅度的涌入叠在「换了页面」这件事上会显得特别猛；正文之间互切时才给满。
   **历史记录（都试过、都否决）**：
   - 加在整个 `.section__body` 上（整块被压 0.8%、后拖 0.8% 屏）：数值都对（过冲 17%），
     但看不出来 —— 翻页本身就是大位移，0.8% 的小形变被淹没；而且它按速度逐帧跟随，
     形变随速度一起衰减，等到判停时已所剩无几（实测过冲只剩 0.0001）。
   - `elastic` 过冲：位移只给 28px 时过冲仅 1.7px，观感全被缩放盖住；拉到 64px 过冲才可见，
     但那个「弹」正是后来要去掉的。
   结论：幅度要够大、加在**元素**上、且**不要过冲**。
5. **轴判定不能看 `scrollHeight`**：`useOverflowMode` 原用 `scrollHeight > clientHeight` 判断
   「内容装不下」，但**滚动区域会把 transform 造成的溢出算进去** —— 入场位移拉到 640px 后，
   `scrollHeight` 从 982 涨到 1313，于是被判成装不下、轴切成横向、三个 section 重新排列，
   表现为「页面在左右移动」。改为比对 `.section__body` 的 `offsetHeight`（布局尺寸，不含
   transform）加 section 的上下 padding。实测：翻页全程轴不变；把 section 压到 300px
   仍能正确切成横向，恢复后切回纵向。
   （该文件原注释写着「判定与当前模式无关、不会来回抖动」—— 这个前提是被本次位移打破的。）

> `4/ω` 只是「大体到位」的口径（此处约剩 9% 位移），**不等于**视觉停稳时间，后者要更久。

---

## 2026-09-11 · 主页面交互轴：装得下就纵向，装不下就横向

需求先后来了两条：先要求"任何尺寸下都不出现滚动条"，又追加"当会溢出时转为手机逻辑：
左右滑动切换，上下正文可滑动"。后一条其实更好——它把纠结消掉了。

- **装得下** → 纵向整屏切换（滚轮 / 上下键 / 上下滑），正文不滚动。设计稿的默认形态。
- **装不下** → 横向切换（左右滑 / 左右键 / 横向滚轮 / Shift+滚轮），正文纵向自己滚。

判定用 `section.scrollHeight > clientHeight`，任一页超出就整体切到横向模式。
两种模式下 `.section` 的宽高都等于视口，所以几何判定与当前模式无关，**不会来回抖动**。

### 实测

| 视口 | 内容溢出 | 模式 | 页面滚动条 |
|---|---|---|---|
| 3440×1440 / 2560×1080 / 1920×1080 | 0 | 纵向 | 无 |
| 1280×720 | 原 60px → **0**（流式留白修好） | 纵向 | 无 |
| 1024×768 | 功能页 101px | **横向** | 无 |
| 390×844 | 123px | 横向 | 无 |

一行值得记住：**1024×768 的溢出主因是宽度**（列数变少 → 卡片换行变高），所以"装不装得下"
同时取决于宽与高，不能只看高度——表格里 1280×720 反而装得下就是这个原因。

### 否决：整体等比缩放

最初写的是"装不下就把内容 `scale` 到刚好装下"（`--fit-scale` + ResizeObserver）。
能 work，但矮屏会把字缩到难读，而且与纵向/横向两轴是**两套重叠的机制**。
既然横向模式已能优雅承载滚动，缩放就是多余复杂度，已整体删除。

### 配套细节

- 横向模式的手势分工：`|deltaX| > |deltaY|` 才算切页意图，否则一律让给正文滚动（不 preventDefault）。
- 纵向模式保留"正文先滚完再翻页"的让位，但只在 `overflow-y: auto` 时生效。
- 两轴共用同一套临界阻尼弹簧，只是把 transform 从 `translate3d(0, ±N%, 0)` 换成 `translate3d(±N%, 0, 0)`。
- **滚动条只藏轨道、不禁止滚动**（`scrollbar-width: none` + `::-webkit-scrollbar`）。玻璃界面里一条粗滚动条
  非常破坏观感，而横向模式下正文本来就要滚。已验证：滚动条占位宽度 0，滚轮仍能把正文从 0 滚到 101。
  **例外**：后台宽表格 `.table-wrap` 保留滚动条——那里的滚动条是"还有更多列"的必要提示。

---

## 2026-09-11 · 主题色模型（校验脚本抓出的真缺陷）

### 文字色**必须离散**，不能参与插值

背景从浅到深、文字从深到浅，两者**同时连续插值必然在中途亮度相等**，那一瞬间对比度趋近于 0
（实测 19:00→22:00 区间中点对比度仅约 10，文字几乎不可读）。

这不是阈值没调好，是模型错了。修正：文字色改为按背景亮度**离散切换两档**，
阈值取亮度中点 `128` —— 这样最差时刻（背景正好落在阈值上）仍有半个亮度区间，
实测量差 **110.6**（出现在 02:17），远高于可用下限。一天只穿越阈值 **2 次**。

CSS 侧配套：`body` 的背景过渡仍是 4s（保证时段渐变平滑），但**文字色过渡压到 200ms**
—— 文字色是离散切换的，拖长会经过中灰而与背景糊在一起。

### 其他修正

- `fgMuted` 在锚点里是 `rgba()` 格式，却走了 hex 插值 → 产出 `#NaNNaNNaN`。现改为离散两档直接给出。
- 颜色统一**大写**：锚点色板是大写书写，插值结果必须能与之逐字符比对。

### 已固化的数值

| 不变量 | 实测 | 阈值 |
|---|---|---|
| 相邻 1 分钟最大通道差 | 2（05:02） | ≤ 3 |
| 凌晨 / 中午背景亮度 | 28.4 / 250.5 | 夜间须明显更暗 |
| 最差前景背景亮度差 | 110.6（02:17） | > 100 |
| 文字色一天切换次数 | 2 | ≤ 2 |

### 待决（留给你定）

**19:00 → 22:00 之间约 1.5 小时背景处于中灰**：20:49 实测 `#747686`（亮度 119）。
这是"晚上(浅) → 午夜(深)"线性插值的必然结果。若觉得这段发闷，可以在 **20:30** 加一个
已经转深的锚点，把入夜压缩到更短时间；代价是夜间变色的"渐变感"会减弱。
用 `reference/palette-review.html` 拖时间轴即可对比。

---

## 2026-09-11 · 临时：管理员密码固定为 `123`

本地模拟期间要求使用固定口令。做法与代价：

- `docker-compose.yml` 的 `ADMIN_PASSWORD` 默认值由「留空 → 随机生成」改为 `123`（仍可用环境变量覆盖）。
- 该变量**只在账号不存在时生效**，所以换口令的实际操作是：删掉 `users` 里的 admin 行 → 重启容器 → 冷启动重建。
- 已验证：`123` 登录 200，旧随机口令 401。
- **代价（务必记住）**：`123` 只有 3 位，不满足应用自身对注册用户的 ≥ 8 位要求 —— 管理员创建路径不做强度校验，
  这是一处已知的不一致。**对外部署前必须用环境变量覆盖成强口令**，且该默认值会随仓库公开。
- 正经做法是加一个改密接口，而不是靠删行重建。

---

## 授权边界与禁止项

- **不改** agent 配置与核心配置文件。
- 管理后台放在 `/admin` 独立路径，**不占用** `design.md` 2.5 规定的三个侧栏入口。
- 三页文案必须来自 `src/content/site.ts`，**不得**硬编码进组件。
- 视觉语言以 `design.md` 2.1（浅色系玻璃）为准；RhineLabUI 只借**机制**（运动数学、对照页、
  决策日志形态），不借**皮肤**（近黑暖灰、终端等宽排字、扫描线）。
- 未提即不做。

---

## 变更后必须跑的三件事

```bash
cd frontend && npm run check     # 内容 / 运动数学 / 时段主题，共 21 项断言（build 前自动执行）
cd frontend && npm run build     # tsc --noEmit + vite build
cd backend  && go test ./tests/... -count=1   # 24 项，含 HTTP 集成测试
```

改完动效或配色，另外打开 `http://localhost:5173/reference/palette-review.html` 看实时读数
（该页只在开发服务器存在，不进入生产构建）。
